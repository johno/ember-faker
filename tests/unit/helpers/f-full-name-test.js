import {
  fFullName
} from '../../../helpers/f-full-name';
import { module, test } from 'qunit';

module('FFullNameHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fFullName(42);
  assert.ok(result);
});
