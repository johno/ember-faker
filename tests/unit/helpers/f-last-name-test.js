import {
  fLastName
} from '../../../helpers/f-last-name';
import { module, test } from 'qunit';

module('FLastNameHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fLastName(42);
  assert.ok(result);
});
