import {
  fFirstName
} from '../../../helpers/f-first-name';
import { module, test } from 'qunit';

module('FFirstNameHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fFirstName(42);
  assert.ok(result);
});
